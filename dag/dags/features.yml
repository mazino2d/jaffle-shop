name: features
description: "ML feature tables: RFM, AOV, return_rate."
dbt_selector: "tag:features"

trigger:
  type: asset_sensor
  depends_on: core

catchup:
  enabled: false

retry:
  max_retries: 1
  retry_delay_minutes: 10
