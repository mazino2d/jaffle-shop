name: raw_ingestion
description: "Ingest data from backend (Faker) into DuckDB raw schema via dlt."

trigger:
  type: cron
  cron_schedule: "0 6 * * *"  # daily at 6am

catchup:
  enabled: true
  start_date: "2024-01-01"

retry:
  max_retries: 3
  retry_delay_minutes: 5
