name: core
description: "Dimension and fact tables for reporting."
dbt_selector: "tag:core"

trigger:
  type: asset_sensor
  depends_on: intermediate

catchup:
  enabled: false

retry:
  max_retries: 1
  retry_delay_minutes: 10
