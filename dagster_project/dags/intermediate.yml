name: intermediate
description: "Business logic joins across staging models."
dbt_selector: "tag:intermediate"

trigger:
  type: asset_sensor
  depends_on: staging

catchup:
  enabled: true
  start_date: "2024-01-01"

retry:
  max_retries: 2
  retry_delay_minutes: 5
